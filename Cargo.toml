[package]
name = "point_process"
version = "0.1.2"
authors = ["ManifoldFR <manifoldfr@outlook.com>"]
description = "A crate for simulating random point processes."
license = "MIT"
readme = "README.md"
exclude = [
    "test.*"
]

[dependencies]
rand = "^0.5"

[dev-dependencies]
gnuplot = "*"

[lib]
name = "pointprocesses"
path = "src/lib.rs"
